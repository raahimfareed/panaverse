#!/usr/bin/env node

// This code is from program 9

// A number variable which is constant - cannot be changed
const favNum = 404;

// A message variable which has the number interpolated in it
const bigReveal = `My favorite number is... *Dramatic Music*... ${favNum}`;

// Output to terminal - stdout
console.log(bigReveal);
